# 数据库配置
DB_CONFIG = {
    'host': 'localhost',
    'port': 33069,
    'user': 'root',
    'password': 'root',
    'database': 'power_management',
    'charset': 'utf8mb4'
}

# Excel配置
EXCEL_CONFIG = {
    'default_sheet': 0,  # 默认读取第一个sheet，可以改为名称如 'Sheet1'
    'header_row': 0,     # 表头所在行（0-based）
}